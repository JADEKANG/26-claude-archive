# JTech 장애 리포트 — 2026-07-05

## 무슨 일이 있었나
- **09:00:04** launchd 정상 발화, 브리프 생성 시작.
- **09:22:27** `claude -p` 헤드리스 세션이 Anthropic API 일시 장애로 중단 (`API Error: Connection closed mid-response`) → exit 1 FAILED.
- **09:22:28** 스크립트가 설계대로 채널에 "⚠️ 발송 실패" 경고 발송 — 제이드가 본 에러가 이것.
- **20:30:25** 수동 재실행 (`launchctl kickstart`) → **20:35:28 SUCCESS**, 당일 브리프 정상 도착 (6개 스토리 / 4개 카테고리).

## 원인
스크립트·launchd·웹훅 문제 아님. **Anthropic API 커넥션이 생성 도중 끊긴 일시적(transient) 장애.** 기존 스크립트는 1회 시도 후 바로 포기하는 구조였고, "다음 wake 때 재시도"는 Mac이 실제로 잠들었다 깨어나야만 발동해서 이날처럼 계속 켜져 있으면 수동 개입 전까지 미발송 상태로 남음.

## 재발 방지 (2026-07-05 적용)
`run-jtech.sh`에 **자가 복구 재시도 루프** 추가:
- 최대 **3회 시도**, 실패 시 **5분 대기** 후 재시도 (일시 장애는 보통 수 분 내 해소).
- 3회 모두 실패했을 때만 기존 실패 알림 발송 → 알림이 오면 "일시 장애가 아니라 진짜 문제"라는 뜻.
- 성공 시 즉시 루프 탈출, `last_success` 스탬프 — 하루 1회 발송 보장은 그대로.

## 검증
- `zsh -n` 문법 검사 통과.
- 샌드박스 테스트 (실제 웹훅 미사용): ① 항상 실패하는 명령 → 3회 시도 + 대기 + FAILED 기록 확인. ② 성공 명령 → 1회 만에 SUCCESS + 스탬프 기록 확인.
- 오늘자 브리프는 이미 발송 완료 상태라 실전 검증은 내일 09:00 정기 실행이 첫 회차.

## 남는 한계
- API 장애가 15분 이상 지속되면 여전히 실패 → 실패 알림 후 수동 `launchctl kickstart gui/$(id -u)/net.eoeoeo.jtech` (단, `rm ~/.claude/jtech/last_success` 불필요 — 실패한 날은 스탬프가 안 찍힘).
- Mac이 하루 종일 꺼져 있으면 그날 발송 불가 (기존과 동일).
